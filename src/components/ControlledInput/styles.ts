import { RFValue } from "react-native-responsive-fontsize";
import styled from "styled-components/native";

export const Error = styled.Text`
  color: #dc1637;
  margin: 0 0 0 ${RFValue(20)}px;
  font-size: ${RFValue(10)}px;
`;
